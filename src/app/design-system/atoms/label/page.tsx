import { LabelPageClient } from "./LabelPageClient"

export const metadata = {
    title: "Label | Design System",
    description: "Label 컴포넌트 문서 및 실시간 플레이그라운드",
}

export default function LabelPage() {
    return <LabelPageClient />
}
