import { ScrollAreaPageClient } from "./ScrollAreaPageClient"

export const metadata = {
    title: "ScrollArea | Design System",
    description: "ScrollArea component documentation",
}

export default function ScrollAreaPage() {
    return <ScrollAreaPageClient />
}
